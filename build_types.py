from enum import Enum
class BuildTypes(Enum): # choose one from here
    MELEE = 0
    SPELL = 1
    HYBRID = 2
class BuildPlaystyles(Enum): # choose one from here
    HEAVY = 0
    LIGHT = 1
class BuildAttributes(Enum): # choose one from here
    DEFENSIVE = 0
    OFFENSIVE = 1

